# LISP-Interpreter
This project involves the development of a LISP interpreter implemented in C++. The interpreter will parse and evaluate LISP expressions, enabling arithmetic computation, variable assignment, user-defined functions, and interactive execution through a REPL.
